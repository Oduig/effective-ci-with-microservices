pipeline {
  agent any
  stages {
    stage('Build') {
      steps {
        sh 'docker build . -t foo'
      }
    }
    stage('Test') {
      steps {
        // Test
      }
    }
    stage('Distribute') {
      steps {
        // Distribute
      }
    }
  }
}
