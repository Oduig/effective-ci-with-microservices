pipeline {
  agent any
  stages {
    stage('Build') {
      steps {
        sh 'docker build . -t foo'
      }
    }
    stage('Test') {
      steps {
        echo 'Testing...'
      }
    }
    stage('Distribute') {
      steps {
        echo 'Distributing...'
      }
    }
  }
}
